# README_EXAMPLES

Overview
Add concise usage examples to README.md demonstrating computeDiff on a before and after schema pair and showing both text and JSON formatted outputs.

Example content
- Minimal before and after schema pair illustrating property addition and a type change.
- CLI example showing usage of formatChangesText for human readable output.
- Programmatic example showing import of computeDiff and formatChangesJson for machine consumption.

Acceptance criteria
- README contains at least one before/after example and shows both text and JSON formatter outputs.
- Examples use the public named exports from src/lib/main.js and are copyable into a small script.

Notes
- Keep examples short and focused on the most common developer workflows.
// docs/lib.js
// Self-contained browser module generated by build-web.cjs.
// Identity from package.json; functions extracted from src/lib/main.js.
// Do not edit — regenerate with: npm run build:web
export const name = "repo";
export const version = "0.1.0";
export const description = "";

export function getIdentity() {
  return { name, version, description };
}

export function slugify(input) {
  const s = toStr(input).trim().toLowerCase();
  if (!s) return "";
  // Normalize and remove diacritics
  const normalized = s.normalize ? s.normalize("NFKD") : s;
  const noDiacritics = normalized.replace(/[\u0300-\u036f]/g, "");
  // Remove punctuation except letters/numbers/space/hyphen (Unicode-aware)
  const cleaned = noDiacritics.replace(/[^
\p{L}\p{N}\s-]+/gu, "");
  const slug = cleaned
    .trim()
    .replace(/[\s_]+/g, "-")
    .replace(/-+/g, "-")
    .replace(/^-+|-+$/g, "");
  return slug;
}

export function truncate(input, length = 80, suffix = "…") {
  const s = toStr(input);
  if (!s) return "";
  const max = Number(length) || 0;
  if (max <= 0) return "";
  if (s.length <= max) return s;
  const avail = max - suffix.length;
  if (avail <= 0) return suffix.slice(0, max);
  const candidate = s.slice(0, avail);
  const lastSpace = candidate.lastIndexOf(" ");
  if (lastSpace > 0) {
    return candidate.slice(0, lastSpace) + suffix;
  }
  return candidate + suffix;
}

export function camelCase(input) {
  const s = toStr(input);
  if (!s) return "";
  const parts = s.split(/[^A-Za-z0-9]+/).filter(Boolean);
  if (parts.length === 0) return "";
  const first = parts.shift().toLowerCase();
  const rest = parts
    .map(p => p.charAt(0).toUpperCase() + p.slice(1).toLowerCase())
    .join("");
  return first + rest;
}

export function kebabCase(input) {
  const s = toStr(input);
  if (!s) return "";
  // Insert hyphen between camelCase transitions
  let out = s.replace(/([a-z0-9])([A-Z])/g, "$1-$2");
  // Replace non-alphanumeric with hyphen
  out = out.replace(/[^A-Za-z0-9]+/g, "-").toLowerCase();
  out = out.replace(/-+/g, "-").replace(/^-+|-+$/g, "");
  return out;
}

export function titleCase(input) {
  const s = toStr(input);
  if (!s) return "";
  return s
    .split(/(\s+)/)
    .map(part => {
      // preserve whitespace
      if (/^\s+$/.test(part)) return part;
      return part.charAt(0).toUpperCase() + part.slice(1).toLowerCase();
    })
    .join("");
}

export function wordWrap(input, width = 80) {
  const s = toStr(input);
  if (!s) return "";
  const w = Number(width) || 0;
  if (w <= 0) return s;
  const words = s.split(/\s+/).filter(Boolean);
  const lines = [];
  let current = "";
  for (const word of words) {
    if (!current) {
      if (word.length > w) {
        lines.push(word);
      } else {
        current = word;
      }
    } else {
      if (current.length + 1 + word.length <= w) {
        current = current + " " + word;
      } else {
        lines.push(current);
        if (word.length > w) {
          lines.push(word);
          current = "";
        } else {
          current = word;
        }
      }
    }
  }
  if (current) lines.push(current);
  return lines.join("\n");
}

export function stripHtml(input) {
  const s = toStr(input);
  if (!s) return "";
  // Remove script/style content
  let out = s.replace(/<(script|style)[^>]*>[\s\S]*?<\/\1>/gi, "");
  // Remove tags
  out = out.replace(/<[^>]+>/g, "");
  // Decode HTML entities (named and numeric)
  out = out.replace(/&(#x?[0-9a-fA-F]+|[a-zA-Z]+);/g, (match, entity) => {
    if (entity[0] === "#") {
      const isHex = entity[1] && (entity[1].toLowerCase() === "x");
      const num = isHex ? parseInt(entity.slice(2), 16) : parseInt(entity.slice(1), 10);
      return isNaN(num) ? match : String.fromCodePoint(num);
    }
    const map = {
      amp: '&',
      lt: '<',
      gt: '>',
      quot: '"',
      apos: "'",
      nbsp: ' '
    };
    return map[entity] ?? match;
  });
  return out;
}

export function escapeRegex(input) {
  const s = toStr(input);
  if (!s) return "";
  const specials = ['.', '*', '+', '?', '^', '$', '{', '}', '(', ')', '|', '[', ']', '\\', '/'];
  return s.split('').map(c => (specials.includes(c) ? '\\' + c : c)).join('');
}

export function pluralize(input) {
  const s = toStr(input);
  if (!s) return "";
  // preserve case of the root (simple approach)
  const lower = s.toLowerCase();
  if (/(s|x|z|ch|sh)$/i.test(s)) return s + "es";
  if (/[aeiou]y$/i.test(s)) return s + "s"; // vowel + y -> just add s
  if (/y$/i.test(s)) return s.slice(0, -1) + "ies";
  if (/fe$/i.test(s)) return s.slice(0, -2) + "ves";
  if (/f$/i.test(s)) return s.slice(0, -1) + "ves";
  return s + "s";
}

export function levenshtein(a, b) {
  const s = toStr(a);
  const t = toStr(b);
  const n = s.length;
  const m = t.length;
  if (n === 0) return m;
  if (m === 0) return n;
  let prev = new Array(m + 1);
  for (let j = 0; j <= m; j++) prev[j] = j;
  for (let i = 1; i <= n; i++) {
    const cur = [i];
    for (let j = 1; j <= m; j++) {
      const cost = s.charAt(i - 1) === t.charAt(j - 1) ? 0 : 1;
      cur[j] = Math.min(cur[j - 1] + 1, prev[j] + 1, prev[j - 1] + cost);
    }
    prev = cur;
  }
  return prev[m];
}
